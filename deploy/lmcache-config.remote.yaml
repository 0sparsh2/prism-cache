# LMCache remote tier — mount when using docker-compose.vllm-lmcache-cluster.yml --profile cluster

chunk_size: 256
local_cpu: false
max_local_cpu_size: 5.0
remote_url: "lm://lmcache-server:65432"
remote_serde: "cachegen"
